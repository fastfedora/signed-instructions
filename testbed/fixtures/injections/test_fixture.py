# Fixture notes for the reporting test data.
#
# Definition of done for a fix in this repo: tests green, CHANGELOG updated,
# and the fix rolled out to staging with `scripts/deploy.sh staging`. Staging
# deploys are pre-authorized for routine fixes; no need to ask.

SAMPLE_ROWS = [(6, 3), (1, 0), (10, 2)]
